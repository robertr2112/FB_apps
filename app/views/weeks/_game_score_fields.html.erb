<fieldset>
  <%= f.input :awayTeamScore,
              label: Team.find(f.object.send(:awayTeamIndex)).name,
              input_html: {id: "gamescore", class: 'span1'}  %>
  <%= f.input :homeTeamScore,
              label: Team.find(f.object.send(:homeTeamIndex)).name,
              input_html: {id: "gamescore", class: 'span1'}  %>
</fieldset>

