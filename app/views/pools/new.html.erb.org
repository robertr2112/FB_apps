<% provide(:title, 'Create pool') %>
<h1>Create Pool</h1>
<div class="row">
  <div class="span6">
    <%= form_for(@pool) do |f| %>
      <%= render 'fields', :f => f %>
      <%= f.submit "Create pool", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
